export type Image = {
    title: string,
    file_name: string
    image_description: string,
    date_posted: string,
    featured: boolean,
}

export type CommissionType = {id: string, character_name: string, scope: string, com_type:string, details: string, name: string, email:string, date_of_purchase: string, commission_status: string}