import { getAdminByUsername, postAdmin, updateAdminPassword, deleteAdmin} from "./admin";
import {getState, getGallery, toggleState, postImage} from './site'

export default {
    getAdminByUsername,
    postAdmin,
    updateAdminPassword,
    deleteAdmin,
    getState,
    getGallery,
    toggleState,
    postImage
}